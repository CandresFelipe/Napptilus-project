import { createTheme } from '@mui/material';

const theme = createTheme({
  // palette for colors
  palette: {
    primary: {
      main: '#FE6B8B'
    },
    secondary: {
      main: '#fff'
    },
    success: {
      main: '#FF8E53'
    },
    error: {
      main: '#FF5733'
    }
  }
});

export default theme;

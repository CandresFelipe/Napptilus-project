import { createTheme } from '@mui/material';

const theme = createTheme({
  // palette for colors
  palette: {
    primary: {
      main: '#FE6B8B'
    },
    secondary: {
      main: '#fff'
    }
  }
});

export default theme;
